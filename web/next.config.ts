import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the dev-tools badge — the demo may run off `next dev` on a projector.
  devIndicators: false,
};

export default nextConfig;
